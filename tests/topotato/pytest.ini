[pytest]
norecursedirs = .git example-test lib topotato tools etc
addopts = -v
minversion = 6.0

# override with --log-cli-level=DEBUG if needed
log_cli_level = INFO

# EDIT THIS AND UNCOMMENT:
# frr_builddir = ../frr
