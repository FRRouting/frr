int r3-eth0
 ip address 10.0.0.12/29
!
! vrf vrf100
!  vni 100
! exit-vrf
!
router bgp 64001
 no bgp default ipv4-unicast
 !
 neighbor 10.0.0.9 remote-as internal
 neighbor 10.0.0.9 timers 1 3
 neighbor 10.0.0.9 timers connect 1
 !
 address-family l2vpn evpn
  neighbor 10.0.0.9 activate
  advertise-all-vni
 exit-address-family
