int r2-eth0
 ip address 10.0.0.11/29
!
vrf vrf100
 vni 100
exit-vrf
!
int dummy-c1 vrf vrf100
 ip address 10.0.0.1/31
int dummy-c2 vrf vrf100
 ip address 10.0.0.3/31
!
router bgp 64001
 no bgp default ipv4-unicast
 !
 neighbor 10.0.0.9 remote-as internal
 neighbor 10.0.0.9 timers 1 3
 neighbor 10.0.0.9 timers connect 1
 !
 address-family l2vpn evpn
  neighbor 10.0.0.9 activate
  advertise-all-vni
  enable-resolve-overlay-index
 exit-address-family
!
router bgp 64001 vrf vrf100
 no bgp default ipv4-unicast
 no bgp network import-check
 !
 neighbor 10.0.0.0 remote-as external
 neighbor 10.0.0.0 shutdown
 !
 address-family l2vpn evpn
  advertise ipv4 unicast
 exit-address-family
