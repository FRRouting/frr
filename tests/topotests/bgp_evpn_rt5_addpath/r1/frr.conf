int r1-eth0 vrf vrf100
 ip address 10.0.0.1/31
 description c1
!
int r1-eth1 vrf vrf100
 ip address 10.0.0.3/31
 description c2
!
int r1-eth2
 ip address 10.0.0.10/29
!
vrf vrf100
 vni 100
exit-vrf
!
router bgp 64001
 no bgp default ipv4-unicast
 !
 neighbor 10.0.0.9 remote-as internal
 neighbor 10.0.0.9 timers 1 3
 neighbor 10.0.0.9 timers connect 1
 !
 address-family l2vpn evpn
  neighbor 10.0.0.9 activate
  neighbor 10.0.0.9 addpath-tx-all-paths
  !
  advertise-all-vni
 exit-address-family
!
router bgp 64001 vrf vrf100
 no bgp ebgp-requires-policy
 no bgp default ipv4-unicast
 !
 neighbor cs peer-group
 neighbor cs remote-as external
 neighbor cs timers 1 3
 neighbor cs timers connect 1
 !
 neighbor 10.0.0.0 peer-group cs
 neighbor 10.0.0.2 peer-group cs
 !
 address-family ipv4 unicast
  neighbor cs activate
 exit-address-family
 !
 address-family l2vpn evpn
  advertise ipv4 unicast gateway-ip
  rd 10.0.0.10:1
 exit-address-family
