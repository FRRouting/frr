!
interface lo
 ip address 10.0.255.1/32
!
interface eth0
 ip address 10.0.0.1/24
 link-params
  metric 20
  delay 10000
  max-bw 10e+10
  ava-bw 1.25e+08
  enable
  exit-link-params
!
interface eth1
 ip address 10.0.1.1/24
 link-params
  enable
  exit-link-params
!
ip forwarding
!
