log file zebra.log
!
hostname rt1
!
! debug zebra kernel
! debug zebra packet
! debug zebra mpls
!
interface lo
 ip address 1.1.1.1/32
!
interface eth-sw1
 ip address 10.0.1.1/24
 link-params
  enable
  exit-link-params
!
ip forwarding
!
line vty
!
