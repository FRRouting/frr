interface r3-eth0
 ip address 10.113.13.2/30
exit
!
interface r3-eth1
 ip address 10.113.36.1/30
exit
!
interface r3-eth2
 ip address 10.113.113.1/30
exit
!
interface lo
 ip address 10.113.0.3/32
exit
!
router bgp 65501
 bgp router-id 10.113.0.3
 no bgp ebgp-requires-policy
 no bgp reject-as-sets
 bgp confederation identifier 100
 bgp confederation peers 65506
 neighbor 10.113.13.1 remote-as 65501
 neighbor 10.113.36.2 remote-as 65506
 neighbor 10.113.113.2 remote-as 1300
 !
 address-family ipv4 unicast
  network 10.113.0.3/32
  redistribute connected
  maximum-paths 4
  maximum-paths ibgp 4
 exit-address-family
exit
!
end
